# Todo List Web Application

Полнофункциональное веб-приложение для управления задачами с Django бэкендом и красивым фронтендом.

## Функции

- ✅ Регистрация и авторизация пользователей
- ✅ Создание, редактирование и удаление задач
- ✅ Система приоритетов задач (низкий, средний, высокий)
- ✅ Статусы задач (в ожидании, в процессе, завершено)
- ✅ Дедлайны с визуальной индикацией времени
- ✅ Цветовая кодировка задач:
  - 🟢 Зеленый - задача завершена
  - 🔴 Красный - дедлайн истек
  - 🟡 Желтый - осталось меньше суток
  - 🔵 Голубой - задача в процессе
- ✅ Email-уведомления о приближающихся дедлайнах
- ✅ Дашборд со статистикой
- ✅ Адаптивный дизайн

## Требования

- Python 3.8+
- Redis (для Celery)
- SMTP сервер для отправки email (например, Gmail)

## Установка

### 1. Установите Redis

**Windows:**
```bash
# Скачайте Redis для Windows с https://github.com/microsoftarchive/redis/releases
# Или используйте WSL2
```

**Linux/Mac:**
```bash
# Ubuntu/Debian
sudo apt-get install redis-server

# Mac
brew install redis
```

### 2. Установите зависимости Python

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройте переменные окружения

Скопируйте `.env.example` в `.env` и заполните настройки email:

```bash
cp .env.example .env
```

Для Gmail:
1. Включите двухфакторную аутентификацию в Google Account
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Укажите email и App Password в `.env`

```env
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
```

### 4. Выполните миграции

```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Создайте суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

## Запуск приложения

Для полноценной работы приложения нужно запустить три процесса:

### 1. Django сервер

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: http://127.0.0.1:8000

### 2. Redis сервер

```bash
# Windows (если установлен)
redis-server

# Linux/Mac
redis-server
```

### 3. Celery Worker (для обработки задач)

```bash
celery -A todo_project worker -l info
```

### 4. Celery Beat (для периодических задач)

```bash
celery -A todo_project beat -l info
```

**Примечание для Windows:**
На Windows могут быть проблемы с Celery. Используйте:
```bash
celery -A todo_project worker -l info --pool=solo
```

Или используйте WSL2/Docker.

## Использование

1. Откройте http://127.0.0.1:8000
2. Зарегистрируйтесь или войдите в систему
3. Создайте новую задачу с дедлайном
4. Управляйте статусами задач
5. Получайте email-уведомления за 1 час до дедлайна

## Структура проекта

```
todo_project/
├── accounts/              # Приложение для аутентификации
│   ├── forms.py          # Формы регистрации/входа
│   ├── views.py          # Представления
│   └── urls.py           # URL маршруты
├── tasks/                # Приложение для задач
│   ├── models.py         # Модель Task
│   ├── forms.py          # Формы задач
│   ├── views.py          # Представления
│   ├── tasks.py          # Celery задачи
│   ├── admin.py          # Админ панель
│   └── urls.py           # URL маршруты
├── templates/            # HTML шаблоны
│   ├── base.html         # Базовый шаблон
│   ├── accounts/         # Шаблоны авторизации
│   └── tasks/            # Шаблоны задач
├── static/               # Статические файлы
│   ├── css/              # CSS стили
│   └── js/               # JavaScript
├── todo_project/         # Настройки проекта
│   ├── settings.py       # Конфигурация Django
│   ├── urls.py           # Главные URL
│   ├── celery.py         # Конфигурация Celery
│   └── wsgi.py           # WSGI конфигурация
├── manage.py             # Django CLI
└── requirements.txt      # Зависимости
```

## Админ панель

Доступна по адресу: http://127.0.0.1:8000/admin

Здесь можно управлять пользователями и задачами.

## Цветовая система задач

- **Зеленый (success)** - задача завершена
- **Красный (danger)** - дедлайн истек
- **Желтый (warning)** - осталось меньше 24 часов
- **Голубой (info)** - задача в процессе выполнения
- **Светлый (light)** - обычная задача в ожидании

## Email уведомления

Уведомления отправляются автоматически:
- За 1 час до дедлайна
- Когда дедлайн истек

Для работы уведомлений должны быть запущены:
- Redis сервер
- Celery Worker
- Celery Beat

## Технологии

### Backend
- Django 5.0.1
- Django REST Framework
- Celery (асинхронные задачи)
- Redis (брокер сообщений)
- SQLite (база данных)

### Frontend
- Bootstrap 5.3
- Font Awesome 6.4
- jQuery 3.6
- Google Fonts (Poppins)

## Решение проблем

### Redis не запускается на Windows
- Используйте WSL2
- Или скачайте Windows версию: https://github.com/microsoftarchive/redis/releases

### Celery не работает на Windows
```bash
# Используйте параметр --pool=solo
celery -A todo_project worker -l info --pool=solo
```

### Email не отправляются
- Проверьте настройки в `.env`
- Убедитесь, что создали App Password для Gmail
- Проверьте, что запущены Celery Worker и Beat

### Ошибки миграции
```bash
# Удалите базу данных и создайте заново
del db.sqlite3
python manage.py makemigrations
python manage.py migrate
```

## Разработка

Для разработки запустите сервер в режиме отладки:

```bash
python manage.py runserver
```

В `settings.py` установлено `DEBUG = True` для разработки.

**Для продакшена:**
- Установите `DEBUG = False`
- Измените `SECRET_KEY`
- Настройте `ALLOWED_HOSTS`
- Используйте PostgreSQL вместо SQLite
- Настройте статические файлы с помощью WhiteNoise или Nginx

## Лицензия

MIT License

## Автор

Создано с помощью Claude Code
